\usepackage{longtable}

% Create an empty environment that does nothing
% because scrlttr2 does not defin a float table
% TODO: if Quarto ever allows for a tbl-env it
% would make for a cleaner fix!
\newenvironment{table}[1][]{}{}